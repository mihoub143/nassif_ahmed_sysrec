services:
  - type: web
    name: sysrec-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    envVars:
      - key: DATABASE_URL
        value: postgresql://sr2_user:kWG2pVT357HMqGEBULaB5roT6nD4VKQp@dpg-d8he6os2m8qs73b27ehg-a.oregon-postgres.render.com/sr2
